"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function removeFallbacks(objectstyle) {
  const rules = Object.entries(objectstyle).map(([key, value]) => {
    if (typeof value === "string" || typeof value === "number") {
      return [key, value];
    }
    if (Array.isArray(value)) {
      return [key, value.pop()];
    } else {
      return [key, removeFallbacks(value)];
    }
  });
  return Object.fromEntries(rules);
}
exports.default = removeFallbacks;
//# sourceMappingURL=removeFallbacks.js.map
